usage: [python3] [absolute/path/to/]flowpy.py [-h]  -i DATA -g GATE
                  

arguments:
  -h, --help            help message
  -i DATA, --input DATA
                        Path to the csv file containing data from flow cytometry statistics (exported prior to usage)
  -g str, --gate str
                        Gate of interest as drawn on flow cytometer prior to usage